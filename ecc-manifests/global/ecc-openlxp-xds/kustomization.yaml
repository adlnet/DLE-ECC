apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app: ecc-openlxp-xds
  authentication: istio-auth

resources:
- deployment.yaml
- service.yaml
- pvc.yaml

configMapGenerator:
- files:
  - nginx.conf
  name: nginx-default-config
